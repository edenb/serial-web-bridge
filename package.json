{
  "name": "usb-socketio-bridge",
  "version": "1.0.0",
  "description": "USB Socket.IO bridge",
  "main": "server.js",
  "bin": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "serialport": "^9.0.2",
    "socket.io": "^3.0.3"
  }
}
